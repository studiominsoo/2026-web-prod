{
  "name": "studiominsoo-web-dev",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "db:migrate:local": "wrangler d1 execute studiominsoo-web --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute studiominsoo-web --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
